/**
 * From Supabase → Project Settings → API. Both values are designed to ship in
 * client code; row-level security is what keeps each account's rows private.
 * Leave them blank to run without sync — see `supabase/README.md`.
 *
 * Typed as `string` so the "is it configured?" check still compiles once filled in.
 */
export const SUPABASE_URL: string = 'https://dqufxexcwiidimcqcizk.supabase.co';
export const SUPABASE_PUBLISHABLE_KEY: string = 'sb_publishable_oXSX-TCi_vv5dY2iLXb-OA_L9j43u36';
